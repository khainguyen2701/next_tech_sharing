version: "3.9"

services:
  nextjs:
    build: .
    ports:
      - "3000:3000"
    networks:
      - app-network
    restart: always
  nginx:
    image: nginx:1.19-alpine
    ports:
      - "80:80"
    networks:
      - app-network
    restart: always
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - nextjs
networks:
  app-network:
    driver: bridge
