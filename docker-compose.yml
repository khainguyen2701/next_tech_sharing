version: "3.9"

services:
  nextjs:
    build: .
    ports:
      - "3000:3000"
    restart: always
  nginx:
    image: nginx:1.19-alpine
    ports:
      - "80:80"
    restart: always
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - nextjs
networks:
  default:
    driver: bridge
